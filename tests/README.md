# Tests

- **Unit tests** for pure logic (e.g. ramp building, colour list handling) go here and run with `pytest` from the project venv (no QGIS required).
- **Integration tests** that need PyQGIS require running under QGIS’s Python; see [docs/testing.md](../docs/testing.md).
